import { sortingQueryParams } from '../../../helpers/sorting-query-params';
import { module, test } from 'qunit';

module('Unit | Helper | sorting query params');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortingQueryParams(42);
  assert.ok(result);
});
